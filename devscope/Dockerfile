
# Use an official nginx image from the Docker Hub
FROM nginx:latest

# Copy the HTML file into the nginx container's default directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80
